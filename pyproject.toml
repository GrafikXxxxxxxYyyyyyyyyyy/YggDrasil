[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yggdrasil"
version = "0.1.0"
description = "YggDrasil — Lego Diffusion Framework"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.3.0",
    "diffusers>=0.30.0",
    "transformers>=4.40.0",
    "omegaconf>=2.3.0",
    "tqdm",
    "pillow",
    "numpy",
    "accelerate",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["yggdrasil*"]

[tool.setuptools.package-dir]
"" = "."